<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 지도 권한 설정 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MinistopMembershipCategoryActivity" />
        <activity android:name=".EmartMembershipCategoryActivity" />
        <activity android:name=".SevenElevenMembershipCategoryActivity" />
        <activity android:name=".CUMembershipCategoryActivity" />
        <activity android:name=".GSMembershipCategoryActivity" />
        <activity android:name=".MapActivity" />
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="0vc8pp8mux" />

        <activity android:name=".MembershipActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>